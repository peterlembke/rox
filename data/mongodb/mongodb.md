# Mongo database files

Folder "db" will have the collection files. Same folder name as in the container.
Folder "configdb" will have the mongo configuration files. Same folder name as in the container.

You can now see the size of the files and manage them if needed.